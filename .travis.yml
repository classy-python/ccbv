language: python
dist: xenial
sudo: required

python:
  - "3.7"

install:
  - pip install pipenv
  - pipenv sync --dev

script:
  - pipenv run pytest

stages:
  - lint
  - test

jobs:
  include:
    - stage: lint
      script:
        - make lint
